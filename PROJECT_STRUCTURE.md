# 專案結構和檔案功能總結

## 📋 專案概述

這是一個 Flutter 應用程式，主要功能包括：
- 📅 日期行程管理和排程
- 🤖 AI 推薦行程功能
- ⏰ 日程表（時間軸視圖）
- 😴 多元智慧疲勞度追蹤
- 🔄 與後端服務和 Firebase 的資料同步

---

## 📁 專案結構詳細說明

### 🔴 根層檔案

#### `main.dart` - 應用程序執行入口點
**功能**：負責初始化 Firebase 和身份驗證，然後啟動主應用
- Firebase 核心初始化
- 匿名用戶身份驗證
- 全局錯誤處理

#### `widget.dart` - 輸入區塊元件（通用）
**功能**：提供行程輸入表單，包含開始時間、結束時間、任務個數和描述
- 時間選擇功能
- 推薦時間預設
- 動態任務輸入區塊

#### `firebase_options.dart` - Firebase 配置選項
**功能**：集中管理所有平台（Web、Android、iOS、macOS、Windows）的 Firebase 配置

---

### 🟦 核心層（core）

#### `core/app.dart` - 應用 UI 主入口
**功能**：定義 MaterialApp 配置，包括主題、首頁等全局設定
- 應用主題配置
- 首頁路由設定

#### `core/error_handler.dart` - 錯誤處理頁面
**功能**：當應用初始化失敗時顯示錯誤信息和診斷提示
- 錯誤詳情展示
- 診斷檢查清單

---

### 🟪 AI 推薦模組（ai_recommendation）

#### `ai_recommendation/ai_recommendation_page.dart` - AI 推薦行程頁面
**功能**：讓用戶輸入行程需求，與後端 AI 服務通信獲取推薦行程，用戶可選擇和編輯推薦的任務，最後套用到日程表
- 行程需求輸入
- AI 推薦結果展示
- 行程選擇和編輯

#### `ai_recommendation/models/task_model.dart` - 任務模型類
**功能**：定義任務的數據結構，包括事件名稱、時間、持續時間和多元智慧領域等信息
- JSON 序列化和反序列化

#### `ai_recommendation/services/ai_recommendation_service.dart` - AI 推薦服務
**功能**：與後端 AI 服務進行通信，獲取行程推薦和提交用戶選擇的任務

#### `ai_recommendation/utils/task_utils.dart` - 任務工具類
**功能**：提供任務相關的輔助功能
- 事件圖標映射
- 時間格式化
- 時間驗證

#### `ai_recommendation/widgets/ai_input_section.dart` - AI 輸入區塊組件
**功能**：提供文本輸入框讓用戶描述行程需求，支援動畫展開和收合，以及提交按鈕

#### `ai_recommendation/widgets/ai_recommendation_section.dart` - AI 推薦理由展示組件
**功能**：顯示 AI 給出的推薦行程的理由說明，支援展開和收合動畫效果

#### `ai_recommendation/widgets/available_tasks_section.dart` - 可用行程選擇組件
**功能**：展示 AI 推薦的行程列表，用戶可以點擊選擇添加到已選擇列表，提供套用按鈕將選擇的行程應用到日程表

#### `ai_recommendation/widgets/selected_tasks_section.dart` - 已選擇行程展示組件
**功能**：顯示用戶已選擇的行程列表，支援編輯和刪除功能，提供快速預覽每個任務的詳細信息

---

### 🟨 日程表模組（daily_schedule）

#### `daily_schedule/daily_schedule_page.dart` - 日程表頁面
**功能**：主要功能：展示選定日期的所有行程，支援時間軸視圖、行程編輯、刪除、新增等操作，顯示行程時間衝突警告

#### `daily_schedule/models/schedule_model.dart` - 行程數據模型
**功能**：定義行程的數據結構，包括行程名稱、描述、開始時間、結束時間等信息，提供 Firestore 序列化和反序列化方法

#### `daily_schedule/services/schedule_service.dart` - 行程 Firebase 操作服務
**功能**：主要功能：與 Firestore 通信，提供載入、新增、編輯、刪除行程的功能，管理行程的時間和衝突檢測

#### `daily_schedule/utils/schedule_utils.dart` - 行程工具類
**功能**：提供日期和時間格式化功能，用於統一格式化日期和時間字符串

#### `daily_schedule/utils/timeline_utils.dart` - 時間軸工具類
**功能**：提供時間軸視圖的計算和渲染功能，包括時間軸轉像素位置、行程高度計算、時間格線繪製等

#### `daily_schedule/widgets/edit_schedule_dialog.dart` - 行程編輯對話框
**功能**：主要功能：提供編輯行程詳情的對話框，用戶可修改行程名稱、描述、時間等，並第一線保存到 Firebase

#### `daily_schedule/widgets/schedule_actions_dialog.dart` - 行程操作對話框
**功能**：主要功能：市集商業譯輯，清濟、罪棋、編輯、刪除行程等操作的對話框

#### `daily_schedule/widgets/schedule_bar.dart` - 行程棧組件
**功能**：顯示時間軸上的單個行程棧、支援點擊上下事件、複製行程時間構成，支援時間衝突告警

#### `daily_schedule/widgets/schedule_content_adaptive.dart` - 自適應行程內容組件
**功能**：根據可用空間自動調整行程內容的顯示，支援兩欄佈局，供時間軸渲染使用

#### `daily_schedule/widgets/schedule_content.dart` - 行程內容組件
**功能**：顯示日程表中所有行程棧、支援編輯、刪除、上下事件操作，是日程表的中子核心組件

#### `daily_schedule/widgets/schedule_options_dialog.dart` - 行程選項對話框
**功能**：提供底部上滑對話框，羅列行程的推薦、編輯、刪除等操作

#### `daily_schedule/widgets/time_slot_widget.dart` - 時間棅組件
**功能**：顯示時間軸上每個小時的行程，支援編輯、刪除、操作，是時間軸視圖的基本位元素

#### `daily_schedule/widgets/timeline_view.dart` - 時間軸視圖組件
**功能**：顯示日程表的時間軸視圖，支援滑動瀏覽、行程編輯、時間衝突告警等

---

### 🟧 主頁面模組（home_screen）

#### `home_screen/calendar.dart` - 日曆主頁面
**功能**：應用主頁面，展示日曆視圖，用戶可選擇日期查看和管理該日的行程，提供導航到行程創建、AI 推薦等功能

#### `home_screen/custom_bottom_app_bar.dart` - 自定義底部應用欄
**功能**：提供應用主要功能的導航按鈕，包括新增行程、AI 推薦、疲勞度追蹤等功能，顯示應用信息和用戶 UID

#### `home_screen/models/schedule_item.dart` - 行程項目模型
**功能**：定義日曆頁面展示的行程項目數據結構，提供 Firebase 文檔轉換功能和時間排序能力

#### `home_screen/services/calendar_firebase_service.dart` - 日曆 Firebase 服務
**功能**：主要功能：從 Firestore 載入指定日期的行程列表，處理日期格式轉換和行程排序

#### `home_screen/utils/calendar_styles.dart` - 日曆樣式工具類
**功能**：提供日曆視圖的樣式配置，包括日期選擇樣式、標題欄樣式和日期標籤樣式

#### `home_screen/widgets/custom_calendar_widget.dart` - 自定義日曆組件
**功能**：提供複版日曆視圖，用戶可以選擇日期和切換視圖格式（月、週、日）

#### `home_screen/widgets/schedule_list_widget.dart` - 日曆頁面行程列表組件
**功能**：顯示選定日期的行程列表，供用戶點擊進入日程表頁面，支援加載狀態顯示

---

### 🟩 行程創建模組（schedule_creation）

#### `schedule_creation/schedule_creation_page.dart` - 行程創建頁面
**功能**：主要功能：提供手動創建行程的界面，用戶輸入行程基本信息（時間、名稱等），並與後端通信保存到 Firebase

#### `schedule_creation/services/schedule_creation_service.dart` - 行程創建服務
**功能**：主要功能：處理行程創建相關的業務邏輯，與 Firestore 通信保存新行程，支援時間格式化和導航

#### `schedule_creation/services/manual_schedule_service.dart` - 手動行程服務
**功能**：主要功能：賽生日曆 Firebase 服務，提供手動新增、刪除、編輯行程的操作

#### `schedule_creation/utils/time_utils.dart` - 時間工具類
**功能**：提供時間計算功能，包括計算持續時間、智能調整結束時間、驗證時間範圍等功能

#### `schedule_creation/manual_schedule_page_temp.dart` - 手動行程頁面
**功能**：主要功能：提供手動創建行程的界面，支援日期、時間選擇、行程描述、衝突罪棋等功能

#### `schedule_creation/widgets/manual_schedule_form.dart` - 手動行程表單組件
**功能**：主要功能：供用戶輸入行程詳情、選擇時間、提交添加行程、領示衝突查警

---

### 🟥 疲勞度追蹤模組（fatigue）

#### `fatigue/pages/fatigue_page.dart` - 疲勞度頁面
**功能**：主要功能：展示用戶在不同智慧領域的疲勞度數據，支援圖表展示和數據編輯保存，用於分析用戶的疲勞狀況

#### `fatigue/pages/flchart_fristpage.dart` - 疲勞度追蹤選擇頁面
**功能**：主要功能：根據 Howard Gardner 的多元智慧理論，提供配合不同智慧領域的疲勞度追蹤、圖表展示機能

#### `fatigue/widgets/fatigue_chart.dart` - 疲勞度圖表互動組件
**功能**：主要功能：供用戶滑動繪畫一天中每小時的疲勞度曲線、計算疲勞值、標籤、重置等功能

#### `fatigue/widgets/fatigue_chart_layout.dart` - 疲勞度圖表佈局工具
**功能**：提供寶寶寬高欄不同情況下的圖表佈局配置，包括餘白、字體大小等、支援豎亥和縱直方向

---

### 🟦 共享模組（shared）

#### `shared/services/firebase_service.dart` - 共享 Firebase 服務
**功能**：主要功能：提供應用全局的 Firebase 操作，如載入指定日期的行程、管理 Firestore 路徑結構

#### `shared/services/network_service.dart` - 網路服務
**功能**：主要功能：讓應用與後端服務器通信，支援重試機制、長連接、錯誤處理

#### `shared/services/firebase_paths.dart` - Firebase 路徑管理器
**功能**：主要功能：集中管理所有 Firebase Firestore 的路徑結構，確保整應用中路徑一致

#### `shared/utils/schedule_filters.dart` - 行程篩選工具類
**功能**：提供行程數據管篩功能，幫助篩選有效行程、棄卻無效數據等

#### `shared/widgets/schedule_list_widget.dart` - 共享行程列表組件
**功能**：顯示選定日期的行程列表，提供詳情信息、編輯和刪除功能，支援數據篩選

---

## 🔄 數據流架構

```
main.dart (初始化)
    ↓
core/app.dart (UI 入口)
    ↓
home_screen/calendar.dart (主頁)
    ├→ schedule_creation/schedule_creation_page.dart (新增行程)
    ├→ ai_recommendation/ai_recommendation_page.dart (AI 推薦)
    └→ daily_schedule/daily_schedule_page.dart (日程表)
        └→ fatigue/pages/fatigue_page.dart (疲勞度追蹤)
```

---

## 🗂️ Firestore 數據結構

```
Tasks/
├── {userId}/
│   ├── task_list/
│   │   ├── 2024-12-27/
│   │   │   └── tasks/
│   │   │       ├── {docId}: { name, desc, startTime, endTime, ... }
│   │   │       └── ...
│   │   └── ...
│   └── ...
└── ...

profile/
├── {userId}/
│   ├── fatigue_logs/
│   │   └── fatigue_{intelligenceType}: { fatigueData: [...], ... }
│   └── ...
└── ...
```

---

## 🔑 核心功能流程

### 1️⃣ 行程創建流程
- 用戶在 `calendar.dart` 選擇日期
- 點擊新增按鈕 → `schedule_creation_page.dart`
- 用戶輸入行程資訊
- `schedule_creation_service.dart` 保存到 Firestore
- `daily_schedule_page.dart` 自動更新展示

### 2️⃣ AI 推薦流程
- 用戶在 `calendar.dart` 點擊 AI 推薦
- `ai_recommendation_page.dart` 顯示需求輸入
- `ai_recommendation_service.dart` 與後端通信獲取推薦
- 用戶選擇推薦行程
- 通過 `schedule_creation_service.dart` 保存到 Firestore

### 3️⃣ 日程表查看和編輯流程
- 用戶在 `calendar.dart` 點擊日期
- `daily_schedule_page.dart` 展示該日所有行程
- `schedule_service.dart` 從 Firestore 載入數據
- `timeline_view.dart` 以時間軸形式渲染行程
- 用戶可編輯、刪除或新增行程

### 4️⃣ 疲勞度追蹤流程
- 用戶點擊疲勞度按鈕
- `flchart_fristpage.dart` 顯示智慧領域選擇
- `fatigue_page.dart` 展示和編輯疲勞數據
- `fatigue_chart.dart` 供用戶繪製疲勞度曲線
- 數據保存到 Firestore 的 `profile/{userId}/fatigue_logs`

---

## 📝 編碼慣例

- **Page 檔案**：代表完整的頁面視圖，通常是 StatefulWidget
- **Widget 檔案**：代表可複用的組件，通常是 StatelessWidget
- **Service 檔案**：代表業務邏輯和 API 通信層
- **Model 檔案**：代表數據結構和數據轉換
- **Utils 檔案**：代表輔助工具函數和常量

---

## 🛠️ 核心依賴

- **firebase_core**：Firebase 核心初始化
- **firebase_auth**：用戶身份驗證
- **cloud_firestore**：實時數據庫
- **flutter**：UI 框架
- **table_calendar**：日曆組件
- **http**：網路請求
- **fl_chart**：圖表展示

---

## 📌 後續改進建議

1. 🔒 增強 Firebase 安全規則
2. 📊 優化疲勞度演算法
3. 🎨 改進 UI/UX 設計
4. ⚡ 添加離線同步功能
5. 🧪 增加單元測試和集成測試
6. 📈 添加分析和埃點功能
7. 🌐 支持多語言

---

**最後更新**：2025 年 12 月 27 日
**專案規模**：約 30+ Dart 檔案，5000+ 行代碼
